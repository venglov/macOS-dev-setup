add_newline = true
# Avoid requiring a patched font; preserve Starship's other defaults.
format = "$directory$git_branch$git_status$cmd_duration$line_break$character"

[git_branch]
symbol = "git "

[character]
success_symbol = "[>](bold green)"
error_symbol = "[>](bold red)"
